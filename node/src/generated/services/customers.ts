// Auto-generated by scripts/generate.ts — do not edit manually

import { BaseService } from "../../services/base";
import type {
  Customer,
  CustomerCreateParams,
  CustomerListParams,
  CustomerUpdateParams,
} from "../types/customers";
import {
  SORT_DIRECTION_TO_PROTO,
} from "../types/common";
import type { FlintList } from "../../pagination";
import {
  CUSTOMER_SORT_FIELD_TO_PROTO,
} from "../types/customers";

const SERVICE = "flint.v1.customers.CustomerService";

export class CustomerService extends BaseService {
  async create(params: CustomerCreateParams): Promise<Customer> {
    const res = await this.rpc<Record<string, unknown>, { customer: Raw }>(
      SERVICE,
      "CreateCustomer",
      {
        name: params.name,
        email: params.email,
        billingAddress: params.billingAddress ? toProtoAddress(params.billingAddress) : undefined,
        shippingAddress: params.shippingAddress ? toProtoAddress(params.shippingAddress) : undefined,
        merchantCustomerId: params.merchantCustomerId,
        buyerNote: params.buyerNote,
        phoneNumber: params.phoneNumber,
        metadata: params.metadata,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
        merchantNote: params.merchantNote,
        taxExempt: params.taxExempt,
      }
    );
    return fromRawCustomer(res.customer);
  }

  async get(customerId: string): Promise<Customer> {
    const res = await this.rpc<Record<string, unknown>, { customer: Raw }>(
      SERVICE,
      "GetCustomer",
      { customerId }
    );
    return fromRawCustomer(res.customer);
  }

  async getByEmail(email: string): Promise<Customer> {
    const res = await this.rpc<Record<string, unknown>, { customer: Raw }>(
      SERVICE,
      "GetCustomerByEmail",
      { email: email }
    );
    return fromRawCustomer(res.customer);
  }

  async update(customerId: string, params: CustomerUpdateParams): Promise<Customer> {
    const res = await this.rpc<Record<string, unknown>, { customer: Raw }>(
      SERVICE,
      "UpdateCustomer",
      {
        customerId,
        name: params.name,
        billingAddress: params.billingAddress ? toProtoAddress(params.billingAddress) : undefined,
        shippingAddress: params.shippingAddress ? toProtoAddress(params.shippingAddress) : undefined,
        merchantCustomerId: params.merchantCustomerId,
        buyerNote: params.buyerNote,
        phoneNumber: params.phoneNumber,
        metadata: params.metadata,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
        merchantNote: params.merchantNote,
        clearBillingAddress: params.clearBillingAddress,
        clearShippingAddress: params.clearShippingAddress,
        taxExempt: params.taxExempt,
      }
    );
    return fromRawCustomer(res.customer);
  }

  list(params: CustomerListParams = {}): FlintList<Customer> {
    return this.paginate((pageToken) => this.fetchPage(params, pageToken));
  }

  private async fetchPage(params: CustomerListParams, pageToken?: string) {
    const res = await this.rpc<
      Record<string, unknown>,
      { customers?: Raw[]; nextPageToken?: string }
    >(SERVICE, "ListCustomers", {
      query: params.query,
      pageSize: params.limit,
      pageToken: pageToken ?? params.pageToken,
      sortField: params.sortField ? CUSTOMER_SORT_FIELD_TO_PROTO[params.sortField] : undefined,
      sortDirection: params.sortDirection ? SORT_DIRECTION_TO_PROTO[params.sortDirection] : undefined,
      createdAfter: this.toTimestamp(params.createdAfter),
      createdBefore: this.toTimestamp(params.createdBefore),
      updatedAfter: this.toTimestamp(params.updatedAfter),
      updatedBefore: this.toTimestamp(params.updatedBefore),
      email: params.email,
    });

    return this.toPage(
      (res.customers ?? []).map(fromRawCustomer),
      res.nextPageToken
    );
  }

}

// ============================================================================
// Proto ↔ Clean type converters
// ============================================================================

type Raw = Record<string, unknown>;

const fromRawCustomer = (raw: Raw): Customer => ({
  customerId: raw["customerId"] as string,
  name: raw["name"] as string,
  email: raw["email"] as string,
  billingAddress: raw["billingAddress"] ? fromRawAddress(raw["billingAddress"] as Raw) : undefined,
  shippingAddress: raw["shippingAddress"] ? fromRawAddress(raw["shippingAddress"] as Raw) : undefined,
  merchantCustomerId: raw["merchantCustomerId"] as string | undefined,
  buyerNote: raw["buyerNote"] as string | undefined,
  phoneNumber: raw["phoneNumber"] as string | undefined,
  metadata: (raw["metadata"] as Record<string, string>) ?? {},
  merchantId: raw["merchantId"] as string,
  createdAt: new Date(raw["createdAt"] as string),
  updatedAt: new Date(raw["updatedAt"] as string),
  merchantNote: raw["merchantNote"] as string | undefined,
  defaultPaymentMethodId: raw["defaultPaymentMethodId"] as string | undefined,
  taxExempt: raw["taxExempt"] as boolean | undefined,
});

const fromRawAddress = (raw: Raw) => ({
  line1: raw["line1"] as string,
  line2: raw["line2"] as string | undefined,
  country: raw["country"] as string,
  city: raw["city"] as string,
  state: raw["state"] as string,
  postalCode: raw["postalCode"] as string,
});

const toProtoAddress = (addr: { line1: string; line2?: string; country: string; city: string; state: string; postalCode: string }) => ({
  line1: addr.line1,
  line2: addr.line2,
  country: addr.country,
  city: addr.city,
  state: addr.state,
  postalCode: addr.postalCode,
});
