// Auto-generated by scripts/generate.ts — do not edit manually

import { BaseService, resolveEnum } from "../../services/base";
import type {
  AdjustInventoryParams,
  InventoryLogEntry,
  Item,
  ItemCreateParams,
  ItemListParams,
  ItemUpdateParams,
  ReplaceItemCategoriesParams,
} from "../types/items";
import {
  SORT_DIRECTION_TO_PROTO,
} from "../types/common";
import type { FlintList } from "../../pagination";
import {
  INVENTORY_CHANGE_REASON_MAP,
  INVENTORY_CHANGE_REASON_TO_PROTO,
  ITEM_SORT_FIELD_TO_PROTO,
  ITEM_STATUS_MAP,
  ITEM_STATUS_TO_PROTO,
  ITEM_TYPE_MAP,
  ITEM_TYPE_TO_PROTO,
} from "../types/items";

const SERVICE = "flint.v1.commerce.ItemService";

export class ItemService extends BaseService {
  async create(params: ItemCreateParams): Promise<Item> {
    const res = await this.rpc<Record<string, unknown>, { item: Raw }>(
      SERVICE,
      "CreateItem",
      {
        name: params.name,
        unitPriceMoney: params.unitPriceMoney,
        type: params.type ? ITEM_TYPE_TO_PROTO[params.type] : undefined,
        description: params.description,
        sku: params.sku,
        enforceInventoryLimit: params.enforceInventoryLimit,
        quantityAvailable: params.quantityAvailable,
        metadata: params.metadata,
        imageUrl: params.imageUrl,
        status: params.status ? ITEM_STATUS_TO_PROTO[params.status] : undefined,
        taxable: params.taxable,
        taxCategory: params.taxCategory,
        enforceInventoryLimitOnSubscriptions: params.enforceInventoryLimitOnSubscriptions,
        categories: params.categories,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
      }
    );
    return fromRawItem(res.item);
  }

  async get(itemId: string): Promise<Item> {
    const res = await this.rpc<Record<string, unknown>, { item: Raw }>(
      SERVICE,
      "GetItem",
      { itemId }
    );
    return fromRawItem(res.item);
  }

  async getBySKU(sku: string): Promise<Item | null> {
    const res = await this.rpc<Record<string, unknown>, { item?: Raw }>(
      SERVICE,
      "GetItemBySKU",
      { sku }
    );
    return res.item ? fromRawItem(res.item) : null;
  }

  async update(itemId: string, params: ItemUpdateParams): Promise<Item> {
    const res = await this.rpc<Record<string, unknown>, { item: Raw }>(
      SERVICE,
      "UpdateItem",
      {
        itemId,
        name: params.name,
        unitPriceMoney: params.unitPriceMoney,
        type: params.type ? ITEM_TYPE_TO_PROTO[params.type] : undefined,
        description: params.description,
        sku: params.sku,
        enforceInventoryLimit: params.enforceInventoryLimit,
        enforceInventoryLimitOnSubscriptions: params.enforceInventoryLimitOnSubscriptions,
        metadata: params.metadata,
        imageUrl: params.imageUrl,
        status: params.status ? ITEM_STATUS_TO_PROTO[params.status] : undefined,
        taxable: params.taxable,
        taxCategory: params.taxCategory,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
      }
    );
    return fromRawItem(res.item);
  }

  async replaceItemCategories(itemId: string, params: ReplaceItemCategoriesParams = {}): Promise<Item> {
    const res = await this.rpc<Record<string, unknown>, { item: Raw }>(
      SERVICE,
      "ReplaceItemCategories",
      {
        itemId,
        categories: params.categories,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
      }
    );
    return fromRawItem(res.item);
  }

  async del(itemId: string): Promise<Item> {
    const res = await this.rpc<Record<string, unknown>, { item: Raw }>(
      SERVICE,
      "DeleteItem",
      { itemId }
    );
    return fromRawItem(res.item);
  }

  async adjustInventory(itemId: string, params: AdjustInventoryParams): Promise<Item> {
    const res = await this.rpc<Record<string, unknown>, { item: Raw }>(
      SERVICE,
      "AdjustInventory",
      {
        itemId,
        quantityDelta: params.quantityDelta,
        note: params.note,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
        changeReason: params.changeReason ? INVENTORY_CHANGE_REASON_TO_PROTO[params.changeReason] : undefined,
      }
    );
    return fromRawItem(res.item);
  }

  list(params: ItemListParams = {}): FlintList<Item> {
    return this.paginate((pageToken) => this.fetchPage(params, pageToken));
  }

  private async fetchPage(params: ItemListParams, pageToken?: string) {
    const res = await this.rpc<
      Record<string, unknown>,
      { items?: Raw[]; nextPageToken?: string }
    >(SERVICE, "ListItems", {
      pageSize: params.limit,
      pageToken: pageToken ?? params.pageToken,
      type: params.type ? ITEM_TYPE_TO_PROTO[params.type] : undefined,
      status: params.status ? ITEM_STATUS_TO_PROTO[params.status] : undefined,
      onlyInStock: params.onlyInStock,
      category: params.category,
      sortField: params.sortField ? ITEM_SORT_FIELD_TO_PROTO[params.sortField] : undefined,
      sortDirection: params.sortDirection ? SORT_DIRECTION_TO_PROTO[params.sortDirection] : undefined,
      createdAfter: this.toTimestamp(params.createdAfter),
      createdBefore: this.toTimestamp(params.createdBefore),
      updatedAfter: this.toTimestamp(params.updatedAfter),
      updatedBefore: this.toTimestamp(params.updatedBefore),
      readMask: params.readMask ? this.toFieldMask(params.readMask) : undefined,
      query: params.query,
    });

    return this.toPage(
      (res.items ?? []).map(fromRawItem),
      res.nextPageToken
    );
  }

}

// ============================================================================
// Proto ↔ Clean type converters
// ============================================================================

type Raw = Record<string, unknown>;

const fromRawItem = (raw: Raw): Item => ({
  itemId: raw["itemId"] as string,
  name: raw["name"] as string,
  unitPriceMoney: fromRawMoney(raw["unitPriceMoney"] as Raw),
  type: resolveEnum(raw["type"], ITEM_TYPE_MAP, ITEM_TYPE_TO_PROTO) ?? "product",
  description: raw["description"] as string | undefined,
  sku: raw["sku"] as string | undefined,
  enforceInventoryLimit: raw["enforceInventoryLimit"] as boolean | undefined,
  quantityAvailable: raw["quantityAvailable"] != null ? Number(raw["quantityAvailable"]) : undefined,
  metadata: (raw["metadata"] as Record<string, string>) ?? {},
  imageUrl: raw["imageUrl"] as string | undefined,
  status: resolveEnum(raw["status"], ITEM_STATUS_MAP, ITEM_STATUS_TO_PROTO) ?? "active",
  taxable: raw["taxable"] as boolean | undefined,
  taxCategory: raw["taxCategory"] as string | undefined,
  categories: (raw["categories"] as string[]) ?? [],
  inventoryHistory: ((raw["inventoryHistory"] as Raw[]) ?? []).map(fromRawInventoryLogEntry),
  merchantId: raw["merchantId"] as string,
  createdAt: new Date(raw["createdAt"] as string),
  updatedAt: new Date(raw["updatedAt"] as string),
  enforceInventoryLimitOnSubscriptions: raw["enforceInventoryLimitOnSubscriptions"] as boolean | undefined,
});

const fromRawInventoryLogEntry = (raw: Raw): InventoryLogEntry => ({
  quantityDelta: Number(raw["quantityDelta"] ?? 0),
  newQuantity: Number(raw["newQuantity"] ?? 0),
  createdAt: new Date(raw["createdAt"] as string),
  changeReason: resolveEnum(raw["changeReason"], INVENTORY_CHANGE_REASON_MAP, INVENTORY_CHANGE_REASON_TO_PROTO) ?? "manual_adjustment",
  note: raw["note"] as string | undefined,
});

const fromRawMoney = (raw: Raw | undefined) => ({
  amount: Number((raw?.["amount"] as string | number) ?? 0),
  currency: (raw?.["currency"] as string) ?? "USD",
});
