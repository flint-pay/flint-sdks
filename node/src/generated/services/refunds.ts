// Auto-generated by scripts/generate.ts — do not edit manually

import { BaseService, resolveEnum } from "../../services/base";
import type {
  Refund,
  RefundCreateParams,
  RefundListParams,
  RefundUpdateParams,
} from "../types/refunds";
import {
  SORT_DIRECTION_TO_PROTO,
} from "../types/common";
import type { FlintList } from "../../pagination";
import {
  REFUND_METHOD_MAP,
  REFUND_METHOD_TO_PROTO,
  REFUND_REASON_MAP,
  REFUND_REASON_TO_PROTO,
  REFUND_SORT_FIELD_TO_PROTO,
  REFUND_STATUS_MAP,
  REFUND_STATUS_TO_PROTO,
} from "../types/refunds";

const SERVICE = "flint.v1.commerce.RefundService";

export class RefundService extends BaseService {
  async create(params: RefundCreateParams): Promise<Refund> {
    const res = await this.rpc<Record<string, unknown>, { refund: Raw }>(
      SERVICE,
      "CreateRefund",
      {
        reason: params.reason ? REFUND_REASON_TO_PROTO[params.reason] : undefined,
        orderId: params.orderId,
        paymentIntentId: params.paymentIntentId,
        amountMoney: params.amountMoney,
        reasonMessage: params.reasonMessage,
        metadata: params.metadata,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
        refundMethod: params.refundMethod ? REFUND_METHOD_TO_PROTO[params.refundMethod] : undefined,
      }
    );
    return fromRawRefund(res.refund);
  }

  async get(refundId: string): Promise<Refund> {
    const res = await this.rpc<Record<string, unknown>, { refund: Raw }>(
      SERVICE,
      "GetRefund",
      { refundId }
    );
    return fromRawRefund(res.refund);
  }

  async update(refundId: string, params: RefundUpdateParams): Promise<Refund> {
    const res = await this.rpc<Record<string, unknown>, { refund: Raw }>(
      SERVICE,
      "UpdateRefund",
      {
        refundId,
        metadata: params.metadata,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
      }
    );
    return fromRawRefund(res.refund);
  }

  list(params: RefundListParams = {}): FlintList<Refund> {
    return this.paginate((pageToken) => this.fetchPage(params, pageToken));
  }

  private async fetchPage(params: RefundListParams, pageToken?: string) {
    const res = await this.rpc<
      Record<string, unknown>,
      { refunds?: Raw[]; nextPageToken?: string }
    >(SERVICE, "ListRefunds", {
      orderId: params.orderId,
      paymentIntentId: params.paymentIntentId,
      status: params.status ? REFUND_STATUS_TO_PROTO[params.status] : undefined,
      reasons: params.reasons?.map((v: string) => REFUND_REASON_TO_PROTO[v]),
      refundMethod: params.refundMethod ? REFUND_METHOD_TO_PROTO[params.refundMethod] : undefined,
      minAmount: params.minAmount,
      maxAmount: params.maxAmount,
      customerId: params.customerId,
      query: params.query,
      pageSize: params.limit,
      pageToken: pageToken ?? params.pageToken,
      sortField: params.sortField ? REFUND_SORT_FIELD_TO_PROTO[params.sortField] : undefined,
      sortDirection: params.sortDirection ? SORT_DIRECTION_TO_PROTO[params.sortDirection] : undefined,
      createdAfter: this.toTimestamp(params.createdAfter),
      createdBefore: this.toTimestamp(params.createdBefore),
      updatedAfter: this.toTimestamp(params.updatedAfter),
      updatedBefore: this.toTimestamp(params.updatedBefore),
      readMask: params.readMask ? this.toFieldMask(params.readMask) : undefined,
    });

    return this.toPage(
      (res.refunds ?? []).map(fromRawRefund),
      res.nextPageToken
    );
  }

}

// ============================================================================
// Proto ↔ Clean type converters
// ============================================================================

type Raw = Record<string, unknown>;

const fromRawRefund = (raw: Raw): Refund => ({
  refundId: raw["refundId"] as string,
  amountMoney: fromRawMoney(raw["amountMoney"] as Raw),
  status: resolveEnum(raw["status"], REFUND_STATUS_MAP, REFUND_STATUS_TO_PROTO) ?? "pending",
  reason: resolveEnum(raw["reason"], REFUND_REASON_MAP, REFUND_REASON_TO_PROTO) ?? "duplicate",
  reasonMessage: raw["reasonMessage"] as string | undefined,
  metadata: (raw["metadata"] as Record<string, string>) ?? {},
  orderId: raw["orderId"] as string | undefined,
  paymentIntentId: raw["paymentIntentId"] as string | undefined,
  merchantId: raw["merchantId"] as string,
  refundMethod: resolveEnum(raw["refundMethod"], REFUND_METHOD_MAP, REFUND_METHOD_TO_PROTO),
  failureReason: raw["failureReason"] as string | undefined,
  createdAt: new Date(raw["createdAt"] as string),
  updatedAt: new Date(raw["updatedAt"] as string),
  customerId: raw["customerId"] as string | undefined,
  nextActions: ((raw["nextActions"] as Raw[]) ?? []).map(fromRawNextAction),
});

const fromRawMoney = (raw: Raw | undefined) => ({
  amount: Number((raw?.["amount"] as string | number) ?? 0),
  currency: (raw?.["currency"] as string) ?? "USD",
});

const fromRawNextAction = (raw: Raw) => ({
  method: raw["method"] as string,
  reasonCode: raw["reasonCode"] as string | undefined,
  reasonMessage: raw["reasonMessage"] as string | undefined,
  requiredFields: raw["requiredFields"] as string[] | undefined,
  requiresHumanConfirmation: raw["requiresHumanConfirmation"] as boolean | undefined,
  suggestedDelayMs: raw["suggestedDelayMs"] as number | undefined,
});
