// Auto-generated by scripts/generate.ts — do not edit manually

import { BaseService, resolveEnum } from "../../services/base";
import type {
  Coupon,
  CouponCreateParams,
  CouponListParams,
  CouponUpdateParams,
  ReplaceCouponLimitToItemsParams,
} from "../types/coupons";
import {
  SORT_DIRECTION_TO_PROTO,
} from "../types/common";
import type { FlintList } from "../../pagination";
import {
  COUPON_MUTABLE_STATUS_TO_PROTO,
  COUPON_SORT_FIELD_TO_PROTO,
  COUPON_STATUS_MAP,
  COUPON_STATUS_TO_PROTO,
  DISCOUNT_CALCULATION_BASIS_MAP,
  DISCOUNT_CALCULATION_BASIS_TO_PROTO,
} from "../types/coupons";

const SERVICE = "flint.v1.commerce.CouponService";

export class CouponService extends BaseService {
  async create(params: CouponCreateParams): Promise<Coupon> {
    const res = await this.rpc<Record<string, unknown>, { coupon: Raw }>(
      SERVICE,
      "CreateCoupon",
      {
        name: params.name,
        couponCode: params.couponCode,
        description: params.description,
        maxUses: params.maxUses,
        expiresAt: this.toTimestamp(params.expiresAt),
        status: params.status ? COUPON_MUTABLE_STATUS_TO_PROTO[params.status] : undefined,
        metadata: params.metadata,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
        limitToItemIds: params.limitToItemIds,
        minimumPurchaseAmount: params.minimumPurchaseAmount,
        isStackable: params.isStackable,
        discountCalculationBasis: params.discountCalculationBasis ? DISCOUNT_CALCULATION_BASIS_TO_PROTO[params.discountCalculationBasis] : undefined,
        percentOff: params.percentOff,
        amountOffMoney: params.amountOffMoney,
      }
    );
    return fromRawCoupon(res.coupon);
  }

  async get(couponId: string): Promise<Coupon> {
    const res = await this.rpc<Record<string, unknown>, { coupon: Raw }>(
      SERVICE,
      "GetCoupon",
      { couponId }
    );
    return fromRawCoupon(res.coupon);
  }

  async getByCode(code: string): Promise<Coupon> {
    const res = await this.rpc<Record<string, unknown>, { coupon: Raw }>(
      SERVICE,
      "GetCouponByCode",
      { couponCode: code }
    );
    return fromRawCoupon(res.coupon);
  }

  async update(couponId: string, params: CouponUpdateParams): Promise<Coupon> {
    const res = await this.rpc<Record<string, unknown>, { coupon: Raw }>(
      SERVICE,
      "UpdateCoupon",
      {
        couponId,
        name: params.name,
        couponCode: params.couponCode,
        description: params.description,
        maxUses: params.maxUses,
        expiresAt: this.toTimestamp(params.expiresAt),
        status: params.status ? COUPON_MUTABLE_STATUS_TO_PROTO[params.status] : undefined,
        metadata: params.metadata,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
        minimumPurchaseAmount: params.minimumPurchaseAmount,
        isStackable: params.isStackable,
        discountCalculationBasis: params.discountCalculationBasis ? DISCOUNT_CALCULATION_BASIS_TO_PROTO[params.discountCalculationBasis] : undefined,
        clearMaxUses: params.clearMaxUses,
        clearExpiresAt: params.clearExpiresAt,
        clearMinimumPurchaseAmount: params.clearMinimumPurchaseAmount,
        percentOff: params.percentOff,
        amountOffMoney: params.amountOffMoney,
      }
    );
    return fromRawCoupon(res.coupon);
  }

  async replaceCouponLimitToItems(couponId: string, params: ReplaceCouponLimitToItemsParams = {}): Promise<Coupon> {
    const res = await this.rpc<Record<string, unknown>, { coupon: Raw }>(
      SERVICE,
      "ReplaceCouponLimitToItems",
      {
        couponId,
        limitToItemIds: params.limitToItemIds,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
      }
    );
    return fromRawCoupon(res.coupon);
  }

  async del(couponId: string): Promise<Coupon> {
    const res = await this.rpc<Record<string, unknown>, { coupon: Raw }>(
      SERVICE,
      "DeleteCoupon",
      { couponId }
    );
    return fromRawCoupon(res.coupon);
  }

  list(params: CouponListParams = {}): FlintList<Coupon> {
    return this.paginate((pageToken) => this.fetchPage(params, pageToken));
  }

  private async fetchPage(params: CouponListParams, pageToken?: string) {
    const res = await this.rpc<
      Record<string, unknown>,
      { coupons?: Raw[]; nextPageToken?: string }
    >(SERVICE, "ListCoupons", {
      pageSize: params.limit,
      pageToken: pageToken ?? params.pageToken,
      status: params.status ? COUPON_STATUS_TO_PROTO[params.status] : undefined,
      query: params.query,
      limitToItemId: params.limitToItemId,
      sortField: params.sortField ? COUPON_SORT_FIELD_TO_PROTO[params.sortField] : undefined,
      sortDirection: params.sortDirection ? SORT_DIRECTION_TO_PROTO[params.sortDirection] : undefined,
      createdAfter: this.toTimestamp(params.createdAfter),
      createdBefore: this.toTimestamp(params.createdBefore),
      updatedAfter: this.toTimestamp(params.updatedAfter),
      updatedBefore: this.toTimestamp(params.updatedBefore),
      expiresAfter: this.toTimestamp(params.expiresAfter),
      expiresBefore: this.toTimestamp(params.expiresBefore),
      readMask: params.readMask ? this.toFieldMask(params.readMask) : undefined,
    });

    return this.toPage(
      (res.coupons ?? []).map(fromRawCoupon),
      res.nextPageToken
    );
  }

}

// ============================================================================
// Proto ↔ Clean type converters
// ============================================================================

type Raw = Record<string, unknown>;

const fromRawCoupon = (raw: Raw): Coupon => ({
  couponId: raw["couponId"] as string,
  name: raw["name"] as string,
  couponCode: raw["couponCode"] as string | undefined,
  description: raw["description"] as string | undefined,
  maxUses: raw["maxUses"] != null ? Number(raw["maxUses"]) : undefined,
  usesCount: raw["usesCount"] != null ? Number(raw["usesCount"]) : undefined,
  expiresAt: raw["expiresAt"] ? new Date(raw["expiresAt"] as string) : undefined,
  status: resolveEnum(raw["status"], COUPON_STATUS_MAP, COUPON_STATUS_TO_PROTO) ?? "active",
  metadata: (raw["metadata"] as Record<string, string>) ?? {},
  limitToItemIds: (raw["limitToItemIds"] as string[]) ?? [],
  minimumPurchaseAmount: raw["minimumPurchaseAmount"] ? fromRawMoney(raw["minimumPurchaseAmount"] as Raw) : undefined,
  isStackable: raw["isStackable"] as boolean,
  discountCalculationBasis: resolveEnum(raw["discountCalculationBasis"], DISCOUNT_CALCULATION_BASIS_MAP, DISCOUNT_CALCULATION_BASIS_TO_PROTO) ?? "subtotal_pre_tax",
  merchantId: raw["merchantId"] as string,
  createdAt: new Date(raw["createdAt"] as string),
  updatedAt: new Date(raw["updatedAt"] as string),
  percentOff: raw["percentOff"] as number | undefined,
  amountOffMoney: raw["amountOffMoney"] ? fromRawMoney(raw["amountOffMoney"] as Raw) : undefined,
});

const fromRawMoney = (raw: Raw | undefined) => ({
  amount: Number((raw?.["amount"] as string | number) ?? 0),
  currency: (raw?.["currency"] as string) ?? "USD",
});
