// Auto-generated by scripts/generate.ts — do not edit manually

import { BaseService, resolveEnum } from "../../services/base";
import type {
  CancelSubscriptionParams,
  PauseSubscriptionParams,
  Subscription,
  SubscriptionCreateParams,
  SubscriptionListParams,
  SubscriptionUpdateParams,
} from "../types/subscriptions";
import {
  SORT_DIRECTION_TO_PROTO,
} from "../types/common";
import type { FlintList } from "../../pagination";
import {
  SUBSCRIPTION_SORT_FIELD_TO_PROTO,
  SUBSCRIPTION_STATUS_MAP,
  SUBSCRIPTION_STATUS_TO_PROTO,
} from "../types/subscriptions";

const SERVICE = "flint.v1.commerce.SubscriptionService";

export class SubscriptionService extends BaseService {
  async create(params: SubscriptionCreateParams): Promise<Subscription> {
    const res = await this.rpc<Record<string, unknown>, { subscription: Raw }>(
      SERVICE,
      "CreateSubscription",
      {
        planId: params.planId,
        customerId: params.customerId,
        paymentMethodId: params.paymentMethodId,
        billingAnchorDay: params.billingAnchorDay,
        metadata: params.metadata,
        idempotencyKey: this.ensureIdempotencyKey(params.idempotencyKey),
      }
    );
    return fromRawSubscription(res.subscription);
  }

  async get(subscriptionId: string): Promise<Subscription> {
    const res = await this.rpc<Record<string, unknown>, { subscription: Raw }>(
      SERVICE,
      "GetSubscription",
      { subscriptionId }
    );
    return fromRawSubscription(res.subscription);
  }

  list(params: SubscriptionListParams = {}): FlintList<Subscription> {
    return this.paginate((pageToken) => this.fetchPage(params, pageToken));
  }

  async cancel(subscriptionId: string, params: CancelSubscriptionParams = {}): Promise<Subscription> {
    const res = await this.rpc<Record<string, unknown>, { subscription: Raw }>(
      SERVICE,
      "CancelSubscription",
      {
        subscriptionId,
        cancelImmediately: params.cancelImmediately,
      }
    );
    return fromRawSubscription(res.subscription);
  }

  async pause(subscriptionId: string, params: PauseSubscriptionParams = {}): Promise<Subscription> {
    const res = await this.rpc<Record<string, unknown>, { subscription: Raw }>(
      SERVICE,
      "PauseSubscription",
      {
        subscriptionId,
        pauseDurationCycles: params.pauseDurationCycles,
      }
    );
    return fromRawSubscription(res.subscription);
  }

  async resume(subscriptionId: string): Promise<Subscription> {
    const res = await this.rpc<Record<string, unknown>, { subscription: Raw }>(
      SERVICE,
      "ResumeSubscription",
      { subscriptionId }
    );
    return fromRawSubscription(res.subscription);
  }

  async update(subscriptionId: string, params: SubscriptionUpdateParams): Promise<Subscription> {
    const res = await this.rpc<Record<string, unknown>, { subscription: Raw }>(
      SERVICE,
      "UpdateSubscription",
      {
        subscriptionId,
        paymentMethodId: params.paymentMethodId,
        metadata: params.metadata,
      }
    );
    return fromRawSubscription(res.subscription);
  }

  private async fetchPage(params: SubscriptionListParams, pageToken?: string) {
    const res = await this.rpc<
      Record<string, unknown>,
      { subscriptions?: Raw[]; nextPageToken?: string }
    >(SERVICE, "ListSubscriptions", {
      pageSize: params.limit,
      pageToken: pageToken ?? params.pageToken,
      status: params.status ? SUBSCRIPTION_STATUS_TO_PROTO[params.status] : undefined,
      customerId: params.customerId,
      planId: params.planId,
      readMask: params.readMask ? this.toFieldMask(params.readMask) : undefined,
      sortField: params.sortField ? SUBSCRIPTION_SORT_FIELD_TO_PROTO[params.sortField] : undefined,
      sortDirection: params.sortDirection ? SORT_DIRECTION_TO_PROTO[params.sortDirection] : undefined,
      createdAfter: this.toTimestamp(params.createdAfter),
      createdBefore: this.toTimestamp(params.createdBefore),
      updatedAfter: this.toTimestamp(params.updatedAfter),
      updatedBefore: this.toTimestamp(params.updatedBefore),
      query: params.query,
    });

    return this.toPage(
      (res.subscriptions ?? []).map(fromRawSubscription),
      res.nextPageToken
    );
  }

}

// ============================================================================
// Proto ↔ Clean type converters
// ============================================================================

type Raw = Record<string, unknown>;

const fromRawSubscription = (raw: Raw): Subscription => ({
  subscriptionId: raw["subscriptionId"] as string,
  planId: raw["planId"] as string,
  customerId: raw["customerId"] as string,
  paymentMethodId: raw["paymentMethodId"] as string,
  status: resolveEnum(raw["status"], SUBSCRIPTION_STATUS_MAP, SUBSCRIPTION_STATUS_TO_PROTO) ?? "trialing",
  billingAnchorDay: raw["billingAnchorDay"] as number,
  cancelAtPeriodEnd: raw["cancelAtPeriodEnd"] as boolean,
  currentPeriodStart: new Date(raw["currentPeriodStart"] as string),
  currentPeriodEnd: new Date(raw["currentPeriodEnd"] as string),
  nextBillingDate: new Date(raw["nextBillingDate"] as string),
  trialEnd: raw["trialEnd"] ? new Date(raw["trialEnd"] as string) : undefined,
  contractStartDate: raw["contractStartDate"] ? new Date(raw["contractStartDate"] as string) : undefined,
  contractEndDate: raw["contractEndDate"] ? new Date(raw["contractEndDate"] as string) : undefined,
  pausedAt: raw["pausedAt"] ? new Date(raw["pausedAt"] as string) : undefined,
  canceledAt: raw["canceledAt"] ? new Date(raw["canceledAt"] as string) : undefined,
  metadata: (raw["metadata"] as Record<string, string>) ?? {},
  merchantId: raw["merchantId"] as string,
  createdAt: new Date(raw["createdAt"] as string),
  updatedAt: new Date(raw["updatedAt"] as string),
});
